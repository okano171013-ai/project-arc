# 最新のARCへのフィードバック

現在の最新版：[`docs/reports/Version27_ARC_Feedback.md`](../reports/Version27_ARC_Feedback.md)

Ownerはこのファイルの中身をコピーして、ARC（ChatGPT）との会話に
貼り付けてください。次のVersionが完了するたびに、Claude Codeが
このポインタを更新します。

**Version19以降**：この内容はAgentMessage（`direction: "ToARC"`）
としてもProject ARCへ直接保存されます——ARCがRemote MCP経由で
`agent_message_list`を呼べば、コピペなしで直接読めます
（`docs/handoff/README.md`「経路B」参照）。
